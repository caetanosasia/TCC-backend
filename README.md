# NodeJs server with express

## Available Scripts

In the project directory, you can run:

### `npm install`

to install the dependencies

### `npm run dev`

Runs the app in the development mode.\
Use [http://localhost:3333](http://localhost:3333) to access the local server.
